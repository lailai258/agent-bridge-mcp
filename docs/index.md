# agent-bridge-mcp Documentation Index

**类型：** 单体 MCP service  
**主要语言：** TypeScript  
**架构：** MCP stdio server + 内存进程管理 + 本地进程注册表 + 多 CLI 适配层  
**最后更新：** 2026-05-14

## 项目概览

`agent-bridge-mcp` 是 MCP-only server，用于通过 MCP tools 启动和管理本机 Claude、Codex、Forge、OpenCode、Antigravity CLI 后台任务。项目不直接调用模型 API，不提供人类 CLI 子命令；运行中句柄在内存中，进程元数据和日志路径写入本地注册表。

## 快速参考

- **技术栈：** Node.js、TypeScript、ESM / NodeNext、MCP SDK、Vitest
- **入口点：** `src/server.ts`、`src/app/mcp.ts`、`src/bin/agent-bridge-mcp.ts`
- **构建产物：** `dist/server.js`、`dist/bin/agent-bridge-mcp.js`
- **架构模式：** 分层适配器架构
- **数据库：** 无
- **部署形态：** stdio MCP server
- **唯一 bin：** `agent-bridge-mcp`

## 生成文档

### 核心文档

- [Project Overview](./project-overview.md) - 项目用途、分类、技术栈和关键能力
- [Architecture](./architecture.md) - 模块边界、运行模型、状态语义和安全边界
- [Source Tree Analysis](./source-tree-analysis.md) - 带说明的源码结构
- [Component Inventory](./component-inventory.md) - 主要运行时模块清单
- [Development Guide](./development-guide.md) - 本地开发、测试、发布和排障流程

### 契约与数据

- [MCP Tool Contracts](./mcp-tool-contracts.md) - 对外 MCP tools 契约
- [API Contracts](./api-contracts.md) - MCP API 风格契约与示例
- [Data Models](./data-models.md) - 内存状态、本地注册表与持久化边界

## 现有文档

- [README](../README.md) - 用户安装、MCP 客户端配置、工具列表和模型说明
- [Project Context](../_bmad-output/project-context.md) - AI 代理实现代码时必须遵守的项目规则

## 快速开始

### 安装

```bash
npm install
```

### 构建

```bash
npm run build
```

### 开发启动

```bash
npm run dev
```

### 测试

```bash
npm run test:unit
npm test
```

## AI 辅助开发指引

### 修改 MCP tool

参考：

- `docs/architecture.md`
- `docs/mcp-tool-contracts.md`
- `docs/api-contracts.md`
- `src/app/mcp.ts`
- `src/__tests__/mcp-contract.test.ts`

### 修改 CLI 参数或模型路由

参考：

- `docs/component-inventory.md`
- `src/cli-builder.ts`
- `src/model-catalog.ts`
- `src/__tests__/cli-builder.test.ts`

### 修改进程状态、wait、peek、kill 或 cleanup

参考：

- `docs/data-models.md`
- `src/process-service.ts`
- `src/peek.ts`
- `src/process-result.ts`
- `src/__tests__/process-management.test.ts`
- `src/__tests__/wait.test.ts`
- `src/__tests__/peek.test.ts`

### 修改输出解析

参考：

- `src/parsers.ts`
- `src/__tests__/parsers.test.ts`
- `docs/data-models.md`

### 修改发布入口

参考：

- `package.json`
- `server.json`
- `README.md`
- `docs/development-guide.md`

## 不可忽略约束

- 不要把项目改回“人类 CLI + MCP”双模式。
- 调整本地注册表或日志持久化语义时，必须同步文档和契约测试。
- `run` 必须立即返回 PID，不能等待任务完成。
- `doctor` 只检查二进制路径和可执行性，不检查登录态。
- `peek` 不能返回原始工具输出或累计 stdout/stderr。
- 相对源码导入必须保留 `.js` 后缀。
- `package.json` 只能暴露 `agent-bridge-mcp` bin。

---

_Documentation generated by BMAD Method `document-project` workflow_
